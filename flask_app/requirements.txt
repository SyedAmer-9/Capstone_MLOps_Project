# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
jinja2==3.1.4
python-multipart==0.0.9

# MLflow (for model loading only)
mlflow==2.15.0
dagshub==0.3.34

# ML Core (minimal versions for inference)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.14.0

# NLP
nltk==3.8.1

# AWS (for S3, if needed at runtime)
boto3

# Utilities
python-dotenv
pyyaml
prometheus-client
dill
setuptools